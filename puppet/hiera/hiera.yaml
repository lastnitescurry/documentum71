---

:backends:
  - yaml

:yaml:
  :datadir: "/vagrant/puppet/hiera"

:hierarchy:
#  - "%{machine_env}/%{machine_role}"
#  - "roles/%{role}"

  - "hosts/%{hostname}"
  - roles/applications
  - roles/documentum
  - roles/oracleclient
  - common

:merge_behavior: deeper

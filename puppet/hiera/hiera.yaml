---

:backends:
  - yaml

:yaml:
  :datadir: "/vagrant/puppet/hiera"

:hierarchy:
#  - "%{machine_env}/%{machine_role}"
  - "hosts/%{hostname}"
#  - "roles/%{role}"
  - roles/applications
  - roles/documentum # this is a nasty hard code to demo role seperation on one server
#  - roles/database   # this is a nasty hard code to demo role seperation on one server
  - roles/oracleclient
  - common

:merge_behavior: deeper

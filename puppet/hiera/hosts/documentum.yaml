---

classes:
  - etc_services::hiera

etc_services:
  farrengold/tcp:
    ensure:   present
    port:     '60000'
    comment:  EMC Documentum Repository - farrengold
  farrengold/udp:
    ensure:   present
    port:     '60000'
    comment:  EMC Documentum Repository - farrengold
  farrengold_s/tcp:
    ensure:   present
    port:     '60001'
    comment:  EMC Documentum Repository - farrengold
  farrengold_s/udp:
    ensure:   present
    port:     '60001'
    comment:  EMC Documentum Repository - farrengold

# Check installed
# yum list installed | grep jdk
package:
#  jdk1.8.0_65:
#    ensure:     installed
#    provider:   rpm
#    source:     '/software/Oracle/Java/jdk-8u65-linux-x64.rpm'
# Documentum < DFC 7.2, fails with Java 1.8
  jdk:
    ensure:     1.7.0_79-fcs
    provider:   rpm
    source:     '/opt/software/Oracle/Java/jdk-7u79-linux-x64.rpm'
